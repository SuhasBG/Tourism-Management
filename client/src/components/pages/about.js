import React from "react";

const About = () => {
  return (
    <div className="container">
      <div className="py-4">
        <h1>About Page</h1>
        <p className="lead">
          This project uses OpenMaps API to locate and to calculate the distance
          between the locations and provides the shortest distance between the
          selected points.
        </p>
      </div>
    </div>
  );
};

export default About;
